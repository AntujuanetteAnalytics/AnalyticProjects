   if line:
        parts = line.split()
    if len(parts) != 2:
        print('invalid length')
try:
    number = int(parts[0])
except parts[0] != int(parts[0]):
                   print('invalid data type')
numbers.append(number)
message.append(parts[1])
if not all(numbers[i] == numbers[i-1] + 1 for i in range(1, len(numbers))):
        print('numbers are not consecutive')
ascending_order = sorted(zip(numbers, message))
for i in range(len(sorted_data)):
        row = sorted_data[i][0] * [sorted_data[i][1]]
        Pyramid.extend(row)
       zipped=tuple(zip(sorted_order,Messages))
split_text=split_text.rename(columns={0 : 'Number', 1 : 'Message'})
print(split_text)

sorted_order=tuple(zip(sorted_order,Messages))

sorted_order

sorted_order = []
for i in range(1,len(Numbers)):
    sorted_order.append(i-1+1)
sorted_zip=list(zip(sorted_order,Messages))